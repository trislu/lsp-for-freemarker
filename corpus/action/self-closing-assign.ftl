<#-- Code action: self-closing tag.
     The '/>' close tag on the non-capture <#assign> below produces a
     'self_closing_tag' warning with a quick-fix that replaces '/>' with '>'. -->
<#assign greeting = "Hello" />
