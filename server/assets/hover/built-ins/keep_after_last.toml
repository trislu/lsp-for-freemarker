identifier = "keep_after_last"
category = "built-in"
markdown = """
# keep_after_last
---
> category: [Built-ins for strings](https://freemarker.apache.org/docs/ref_builtins_string.html#ref_builtin_keep_after_last)
---
> __Note:__
> This built-in is available since FreeMarker 2.3.22.

Same as `keep_after`, but keeps the part after the *last* occurrence of the parameter, rather than after the first. Example:
```template
${"foo.bar.txt"?keep_after_last(".")}
```
Output:
```output
txt
```
"""
