<#-- Diagnostic: unexpected <#break>.
     <#break> is only valid inside a <#list> or <#switch> block. Here it is used
     at the top level, which triggers the 'unexpected_break_stmt' error. -->
<#break>
