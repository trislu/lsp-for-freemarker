<#-- Diagnostic: string literal used as an L-value.
     Using a string literal on the left-hand side of <#assign> is syntactically
     valid but discouraged; it triggers the 'string_lvalue' warning. -->
<#assign "dynamic name" = "value">
