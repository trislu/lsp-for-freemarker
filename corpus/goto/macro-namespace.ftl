<#-- Go-to-definition: macro.
     Place the cursor on the `lib` namespace (or the macro name) below and
     trigger go-to-definition to jump to the macro definition. -->
<#macro render>
    <#-- body -->
</#macro>

<#-- Cursor on `render` here jumps to the definition above. -->
<@render />
